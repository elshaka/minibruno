<%= form_for(@base_unit, html: {role: 'form'}) do |f| %>
  <% if @base_unit.errors.any? %>
    <div id="error_explanation">
      <ul>
        <% @base_unit.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :unit, 'Unidad', class: 'control-label' %><br>
    <%= f.text_field :unit, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :description, 'Descripción', class: 'control-label' %><br>
    <%= f.text_field :description, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= link_to 'Regresar', base_units_path, class: 'btn btn-default' %>
    <%= f.submit 'Guardar', class: 'btn btn-success' %>
  </div>
<% end %>