<h1>Unidades base</h1>
<%= render partial: 'layouts/notifications' %>
<table>
  <thead>
    <tr>
      <th>Unidad</th>
      <th>Descripcion</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @base_units.each do |base_unit| %>
      <tr>
        <td><%= base_unit.unit %></td>
        <td><%= base_unit.description %></td>
        <td><%= link_to 'Editar', edit_base_unit_path(base_unit) %></td>
        <td><%= link_to 'Eliminar', base_unit, method: :delete, data: { confirm: '¿Esta seguro?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nueva unidad base', new_base_unit_path %>